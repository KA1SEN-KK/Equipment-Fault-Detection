（一）总体方案与设计思路

本作品面向工业轴承等旋转设备的状态监测需求，针对传统设备维护模式"仅预警无决策"、边缘计算资源有限、诊断结果缺乏可解释性等痛点，构建了一套"多模型并行检测 + AUC加权融合决策 + 大语言模型解释"的三层智能诊断系统。

在方案演进过程中，项目首先基于凯斯西储大学（CWRU）轴承公开数据集对多模型并行检测框架进行了可行性验证，确认了LSTM自编码器、随机森林、孤立森林、One-Class SVM四类模型并行检测的有效性。在此基础上，最终采用NASA CMAPSS涡扇发动机退化数据集作为系统落地的核心数据集，将能力从单纯的故障检测扩展至剩余寿命（RUL）定量预测，构建面向工业预测性维护的完整解决方案。

系统总体架构如下：系统首先通过传感器采集设备运行数据，经预处理后构造标准化输入；输入并行送入四类检测模型，各模型独立输出故障证据；融合层以AUC加权数值融合方式得出综合故障得分，LSTM回归模型独立输出RUL预测值；大语言模型（通义千问）结合两路结果生成可解释的专业维护建议；最终通过可视化前端呈现，辅助运维人员完成维护决策。

本方案的核心设计思路体现在以下三个方面：

① 多模型并行协同检测。单一检测模型存在偏差与盲区，难以覆盖复杂工况下的各类故障模式。本方案采用LSTM自编码器（时序异常检测）、随机森林（有监督故障分类）、孤立森林（离群点检测）及One-Class SVM（超平面异常划分）四类模型并行执行的策略，各模型独立输出证据，有效降低单一模型的误报与漏检风险。

② AUC加权数值融合决策。以各模型在验证集上的AUC-ROC值为权重，对归一化后的模型分数进行加权求和，输出综合故障概率。该方法以实测性能为权重依据，使检测能力更强的模型自动获得更大决策比重，避免经验等权分配带来的偏差。通过阈值扫描优化决策边界，在精确率与召回率之间取得最优平衡。

③ 大语言模型辅助解释。大语言模型不参与核心检测决策，仅在数值融合完成后，结合故障集成得分与RUL预测值，生成包含风险等级判断、具体行动建议和检修时间窗口的专业维护意见，实现"证据可溯、推理透明"的可解释性输出。

本方案已完成核心算法开发与模型训练，正在推进与施耐德EAE平台及MQTT协议的集成工作，具备实际工程落地条件。

（二）技术路线与设计流程

本作品采用分层解耦的系统设计思路，自下而上划分为特征与数据层、模型执行层、领域契约层、融合决策层、应用编排层及表现层，各层职责明确、接口统一，便于模块化开发与系统扩展。

第一步：数据预处理（特征与数据层）

采用NASA CMAPSS FD001数据集，原始数据包含21个传感器通道，通过计算各通道全样本方差，剔除7个近零方差常数传感器，保留14个具有退化信息的有效传感器。采用滑动窗口方法构造样本，窗口大小为30个运行周期，步长为1个周期，单样本形状为30×14。RUL标签采用分段线性处理，将超过125周期的RUL值截断至125，消除发动机早期无明显退化阶段对模型训练的干扰。特征归一化采用StandardScaler，在健康样本上拟合，对全部样本应用。

第二步：多模型并行检测（模型执行层）

将标准化输入并行送入四类检测模型。LSTM自编码器采用编码器-解码器结构，仅在健康样本上训练，以重建误差的99th百分位作为故障判断阈值，通过重建误差大小反映信号偏离正常模式的程度。孤立森林使用100棵隔离树，污染率设为0.05，在健康样本的展平特征上训练，通过样本被隔离的难易程度判断异常。One-Class SVM采用RBF核，nu=0.05，在健康样本上训练，通过超平面划分正常域边界，对训练分布之外的样本输出负值。随机森林为有监督模型，使用全部带标签样本训练，200棵决策树，设置class_weight为balanced以应对样本不平衡问题，直接输出故障概率。各模型独立输出故障证据，互不干扰。

第三步：证据统一格式化（领域契约层）

定义标准化的ModelResult数据结构，将各模型输出结果（标签、得分、原始细节）统一为相同格式的证据对象。通过ModelRunner抽象基类与ModelRegistry注册表，确保模块间解耦，支持检测模型的灵活插拔与扩展。

第四步：AUC加权融合（融合决策层）

在有标签验证集上分别计算四模型的AUC-ROC值，以各模型AUC占总AUC的比例为集成权重。对各模型原始分数进行百分位归一化后加权求和，得到综合故障得分。通过阈值扫描将决策阈值从0.5优化至0.25，将集成召回率从64%提升至84%，F1从76.2%提升至87.5%。RUL预测由独立的LSTM回归模型输出，不参与故障检测融合。

第五步：大语言模型建议生成

融合决策完成后，将综合故障得分、各子模型分数及RUL预测值组织为结构化Prompt，调用通义千问API，输出包含风险等级、具体行动建议和检修时间窗口的专业维护意见。

第六步：结果输出与系统集成（应用编排层与表现层）

诊断结论经应用编排层传递至表现层，通过Streamlit前端实现可视化展示。系统支持通过MQTT协议接收下位机实时上报的传感器数据，诊断结论亦可通过MQTT下发至施耐德EAE平台及PLC控制器，实现与工业控制系统的闭环联动。

离线闭环：训练与评估层

本方案构建了独立的离线训练与评估闭环，在本地完成特征工程、模型训练及超参数优化，生成模型部署产物（.keras、.pkl文件及ensemble_meta.json权重配置）。训练与推理解耦，确保在线系统的轻量化与稳定性。

（三）关键技术要点

本方案的核心技术主要体现在多模型并行检测机制与AUC加权融合决策机制两个方面。

① 多模型并行检测机制

为克服单一检测模型在复杂工况下的偏差与盲区，本方案采用四类模型并行执行的策略，如下表所示：

模型 / 类型 / 作用 / 输出

LSTM自编码器 / 无监督、时序 / 通过重构误差识别时序异常模式 / 异常分数

孤立森林 / 无监督、离群点 / 识别特征空间中的异常样本 / 异常得分

One-Class SVM / 无监督、超平面 / 通过划分正常域边界识别异常 / 决策函数值

随机森林 / 有监督、分类 / 对故障状态进行概率判别 / 故障概率

四个模型独立运行、互不干扰，各自输出故障证据，并行策略可有效降低单一模型的误报与漏检风险，提升检测结果的鲁棒性。

② AUC加权融合决策机制

本方案采用数据驱动的AUC加权数值融合，具体流程如下：

分数归一化：在有标签验证集上计算各模型原始分数的1st和99th百分位，将原始分数线性映射至[0,1]区间；随机森林的predict_proba输出天然处于[0,1]范围无需额外处理。

权重计算：在有标签验证集上分别计算四模型AUC-ROC值，以各模型AUC占总AUC的比例为集成权重，使检测能力更优的模型自动获得更大决策比重。实测四模型AUC均超过0.95，权重接近均等（各约25%）。

阈值优化：对0.25至0.65范围内的阈值以F1为目标进行扫描，最终确定集成得分大于0.25为故障，大于0.45为严重故障，相较初始阈值0.5，F1从76.2%提升至87.5%。

该机制以实测性能为权重依据，无需人工经验分配，融合结果可解释、可复现。

（四）软件设计方案

本作品软件系统采用分层解耦的模块化设计，整体划分为离线训练与在线推理两个相对独立的子系统，便于模型迭代与工程部署。

① 软件架构分层

表现层，模块为可视化前端，功能为实时展示设备健康状态、诊断结果与运维建议，技术实现为Streamlit，支持CMAPSS与CWRU双模式切换。

融合决策层，模块为融合决策引擎，功能为执行AUC加权数值融合及大语言模型维护建议生成，技术实现为ensemble_meta.json权重配置 + 通义千问API。

领域契约层，模块为统一接口层，功能为定义模型输入输出协议，实现模型可插拔，技术实现为ModelRunner抽象基类 + ModelResult / ModelConfig数据类 + ModelRegistry注册表。

模型执行层，模块为多模型并行引擎，功能为并行调用LSTM自编码器、随机森林、孤立森林、One-Class SVM，技术实现为scikit-learn + TensorFlow 2.16 / Keras。

特征与数据层，模块为信号与数据处理，功能为数据读取、滑窗切片、传感器筛选，技术实现为NumPy + Pandas + 自研cmapss_loader。

训练与评估层，模块为离线研发，功能为模型训练、超参数优化、模型验证，技术实现为scikit-learn + TensorFlow 2.16 / Keras + 自研评估脚本。

② 离线训练与在线推理解耦

离线训练基于CMAPSS数据集，在本地完成特征工程、模型训练与验证，生成标准化模型产物（.keras、.pkl文件）及ensemble_meta.json权重配置；在线推理加载预训练模型，运行轻量级推理，大语言模型通过API调用执行建议生成。所有模型产物通过Git进行版本管理，支持快速回滚与迭代更新。

③ 核心代码组织结构

project/
├── training/                       离线训练模块
│   ├── cmapss_fault_detection.py   CMAPSS四模型集成训练
│   ├── cmapss_rul.py               CMAPSS RUL预测训练
│   └── cwru_ensemble.py            CWRU验证阶段训练（历史保留）
├── models/                         模型Runner模块
│   ├── base.py                     抽象基类与数据结构
│   ├── cmapss_lstm_ae_runner.py    CMAPSS集成检测Runner
│   ├── cmapss_rul_runner.py        CMAPSS RUL Runner
│   ├── cwru_ensemble_runner.py     CWRU Runner（历史保留）
│   └── registry.py                 模型注册表
├── agent/                          大模型建议模块
│   ├── llm.py                      LLM接口
│   └── prompts.py                  Prompt模板
├── stream/                         流式推理模块
│   └── engine.py                   滑动窗口流式引擎
├── evaluation/                     评估模块
│   └── cmapss_eval.py              CMAPSS综合评估脚本
├── utils/                          工具模块
│   ├── cmapss_loader.py            CMAPSS数据加载
│   └── data_loader.py              CWRU数据加载
├── transport/                      通信模块
│   └── mqtt_subscriber.py          MQTT数据接入
└── frontend/
└── app.py                      Streamlit双模式界面

④ 软件运行流程

系统支持两种数据输入方式：其一为通过MQTT协议订阅下位机（传感器采集设备或PLC）实时上报的传感器数据，由mqtt_subscriber模块完成接收与解析；其二为直接加载本地CMAPSS测试集文件进行离线批量诊断。数据到位后，系统对传感器数据进行滑窗预处理；四类检测模型并行推理，输出各自故障分数；AUC加权融合层计算综合故障得分，LSTM回归模型输出RUL预测值；调用通义千问API生成维护建议；结果经Streamlit可视化前端呈现，并可通过MQTT协议将诊断结论下发至EAE平台及PLC。

⑤ 关键设计决策

模型可插拔方面，通过统一的ModelRunner抽象基类，新增模型只需实现标准化的predict()方法，即可通过ModelRegistry无缝接入系统。异常降级方面，当大语言模型API不可用时，系统仍可完成全部数值检测与融合，仅维护建议降级为规则生成，确保核心检测功能不中断。

（五）实验设计与数据分析

本研究分两个阶段推进：**第一阶段**基于CWRU振动数据集验证多模型故障检测框架；**第二阶段**迁移至NASA CMAPSS发动机退化数据集，在故障检测基础上新增剩余寿命（RUL）预测能力，实现从"故障识别"到"寿命预测"的功能演进。

---

#### 第一阶段：振动信号故障检测（CWRU数据集）

##### 1. 实验方案

**（1）数据来源**

本阶段采用凯斯西储大学（CWRU）轴承公开数据集。该数据集是轴承故障诊断领域应用最广泛的基准数据集之一，包含正常状态及内圈故障、外圈故障、滚动体故障三类常见故障类型。实验使用12kHz采样频率下的驱动端振动加速度信号。

**（2）实验设置**

| 项目 | 设置 |
| :--- | :--- |
| 采样频率 | 12 kHz |
| 滑窗窗口大小 | 2048 点 |
| 窗口步长 | 1024 点 |
| 故障类型 | 正常、内圈故障、外圈故障、滚动体故障 |
| 同工况评估 | 5折交叉验证（0HP负载） |
| 跨工况评估 | 0HP负载训练，1HP/2HP负载测试 |

**（3）评价指标**

- **准确率（Accuracy）**：整体分类正确的样本比例
- **精确率（Precision）**：预测为正类中实际为正类的比例
- **召回率（Recall）**：实际正类中被正确预测的比例
- **F1分数**：精确率与召回率的调和均值

**（4）实验模型**

本阶段采用四类模型进行对比评估：
- **LSTM自编码器**：无监督时序异常检测模型，通过重构误差识别异常
- **随机森林**：有监督分类模型，输出故障概率分布
- **孤立森林**：无监督异常检测模型，基于隔离树原理
- **One-Class SVM**：无监督异常检测模型，基于超平面划分

##### 2. 实验结果

**（1）同工况异常检测结果（0HP负载，5折交叉验证）**

| 模型 | 准确率 | 精确率 | 召回率 | F1分数 |
| :--- | :--- | :--- | :--- | :--- |
| LSTM自编码器 | 98.64% | 99.97% | 98.66% | 99.31% |
| 随机森林 | 99.87% | 99.97% | 99.87% | 99.93% |
| One-Class SVM | 98.39% | 99.13% | 97.87% | 98.50% |

实验表明，在同工况条件下，各模型均表现出优异的异常检测能力。其中随机森林性能最优，F1分数达到99.93%；LSTM自编码器作为无监督模型，在不依赖标签的情况下仍取得99.31%的F1分数，验证了其时序异常检测的有效性。

**（2）跨工况异常检测结果（0HP→1HP/2HP）**

为验证模型在变工况条件下的泛化能力，以0HP负载数据训练，分别在1HP、2HP负载数据上进行测试。由于CWRU数据集在1HP、2HP工况下的公开数据以故障样本为主，实验重点聚焦于模型的故障检出能力（召回率）。

| 模型 | 1HP召回率 | 2HP召回率 |
| :--- | :--- | :--- |
| 随机森林 | 99.87% | 100% |
| 孤立森林 | 100% | 100% |
| One-Class SVM | 100% | 100% |

跨工况实验结果表明，各模型在不同负载条件下均能有效识别故障样本，召回率均达到或接近100%，验证了所提方案对负载变化的鲁棒性，满足工业现场变工况应用需求。

**（3）模型融合决策效果**

| 融合方式 | 准确率 | 对比单模型最佳 |
| :--- | :--- | :--- |
| 单模型最佳（随机森林） | 99.87% | — |
| 多模型投票融合 | 99.91% | +0.04% |

多模型投票融合通过综合LSTM自编码器、随机森林、孤立森林三者的输出证据，在单模型最佳基础上进一步提升准确率，验证了多模型并行策略的有效性。

##### 3. 阶段小结

CWRU实验验证了多模型并行检测框架的基础可行性，各模型F1分数均超过98.5%，多模型融合进一步提升了准确率。然而，CWRU数据集仅覆盖故障分类任务，无法回答"设备还能运行多久"这一工业现场更关键的问题。为此，第二阶段引入具有完整退化序列标注的NASA CMAPSS数据集，将能力从故障检测扩展至剩余寿命预测。

---

#### 第二阶段：发动机退化检测与剩余寿命预测（CMAPSS数据集）

##### 1. 实验方案

**（1）数据来源**

本阶段采用NASA发布的CMAPSS（Commercial Modular Aero-Propulsion System Simulation）涡扇发动机退化数据集FD001子集。该数据集包含100台发动机从健康到故障的完整退化序列，每台发动机记录21个传感器读数，去除7个近零方差常数传感器后保留14个有效传感器，提供完整的剩余寿命（RUL）真实标签，是RUL预测领域最广泛使用的基准数据集之一。

**（2）实验设置**

| 项目 | 设置 |
| :--- | :--- |
| 有效传感器数 | 14个 |
| 滑窗窗口大小 | 30 周期 |
| 窗口步长 | 1 周期 |
| 故障标签定义 | RUL < 30 周期 = 故障 |
| RUL上限截断 | 125 周期（消除早期无退化阶段影响） |
| 训练集 | 100台发动机完整序列 |
| 测试集 | 100台发动机末段序列 + 真实RUL标签 |

**（3）评价指标**

故障检测部分沿用第一阶段的四项分类指标（Accuracy、Precision、Recall、F1）。RUL预测部分额外引入：

- **MAE**：预测值与真实RUL的平均绝对误差（单位：周期）
- **RMSE**：均方根误差，对大误差更敏感
- **NASA评分**：工业标准评分函数，对偏晚预测（高估剩余寿命）施加更大惩罚，越低越好

$$S = \sum_{i=1}^{N} \begin{cases} e^{-d_i/13}-1, & d_i < 0 \\ e^{d_i/10}-1, & d_i \geq 0 \end{cases}, \quad d_i = \hat{y}_i - y_i$$

**（4）实验模型**

| 任务 | 模型 | 类型 |
| :--- | :--- | :--- |
| 故障检测 | LSTM自编码器 | 无监督，重构误差 |
| 故障检测 | 孤立森林 | 无监督，隔离树 |
| 故障检测 | One-Class SVM | 无监督，超平面 |
| 故障检测 | 随机森林 | 有监督，概率输出 |
| 故障检测 | AUC加权集成 | 四模型融合 |
| RUL预测 | LSTM回归 | 有监督，序列回归 |

相较第一阶段，本阶段在融合策略上引入改进：以各模型在验证集上的AUC值作为集成权重，取代等权投票，使性能更优的模型在决策中占据更大比重；同时通过阈值优化将最终决策阈值从0.5调整至0.25，在精确率与召回率之间取得更优平衡。

##### 2. 实验结果

**（1）故障检测结果（测试集100台发动机，故障率25%）**

| 模型 | 准确率 | 精确率 | 召回率 | F1分数 |
| :--- | :--- | :--- | :--- | :--- |
| LSTM自编码器 | 94.00% | 88.00% | 88.00% | 88.00% |
| 孤立森林 | 88.00% | 72.41% | 84.00% | 77.78% |
| One-Class SVM | 89.00% | 70.59% | 96.00% | 81.36% |
| 随机森林 | 91.00% | 94.44% | 68.00% | 79.07% |
| **AUC加权集成** | **94.00%** | **91.30%** | **84.00%** | **87.50%** |

集成模型在准确率与LSTM自编码器持平（94%）的同时，综合F1分数（87.50%）优于所有单模型，验证了AUC加权融合策略的有效性。各模型AUC均超过0.95，表明CMAPSS FD001的退化特征区分度高。

**（2）RUL回归预测结果**

| 指标 | 数值 |
| :--- | :--- |
| 测试集MAE | 13.15 周期 |
| 测试集RMSE | 17.23 周期 |
| NASA评分 | 618.2（越低越好） |

LSTM回归模型测试集MAE为13.15周期，处于CMAPSS FD001数据集上LSTM基线方法的正常范围（文献典型值12–16周期）。NASA评分618.2表明模型整体偏向保守预测（低估RUL），在工业场景中有助于避免因高估剩余寿命导致的漏检风险。

##### 3. 两阶段对比分析

**（1）任务复杂度演进**

| 维度 | 第一阶段（CWRU） | 第二阶段（CMAPSS） |
| :--- | :--- | :--- |
| 信号类型 | 一维振动信号 | 多变量时序（14传感器） |
| 任务类型 | 故障分类 | 故障检测 + RUL回归 |
| 标签获取 | 人工标注故障类别 | 自然退化序列（真实RUL） |
| 融合策略 | 等权投票 | AUC加权 + 阈值优化 |

**（2）模型性能对比**

CWRU任务中各模型F1均超过98%，而CMAPSS任务中最优模型F1为87.50%。差距原因在于：CWRU为多类故障分类，类间特征差异显著；CMAPSS为退化程度的二值判断，健康与故障之间存在连续过渡区，判断难度更高，更贴近工业实际。

**（3）融合策略改进**

第一阶段采用等权投票融合，各模型贡献相同。第二阶段引入AUC加权机制，使验证集表现更优的模型获得更大权重，并通过阈值扫描将决策阈值从0.5优化至0.25，将集成召回率从64%提升至84%，F1从76.2%提升至87.5%，体现了融合策略的持续优化。

##### 4. 实验结论

本研究两阶段实验得出以下结论：

1. **框架可迁移性**：多模型并行检测框架从CWRU振动信号成功迁移至CMAPSS多变量退化序列，验证了架构的通用性。
2. **故障检测性能**：CMAPSS测试集上，AUC加权集成准确率94%、F1达87.50%，在25%故障率的不平衡数据集上取得了精确率与召回率的优化平衡。
3. **RUL预测能力**：LSTM回归模型测试集MAE为13.15周期，NASA评分618.2，具备实际工程应用价值，能为维护决策提供定量的寿命余量参考。
4. **融合策略验证**：AUC加权集成结合阈值优化，相较初始集成方案F1提升11.3个百分点，证明数据驱动的融合权重优于经验等权分配。

（六）现代工程工具与信息技术工具

本作品在开发过程中，根据各环节的技术需求，合理选用了现代工程工具与信息技术工具。工具选型遵循以下原则：① 成熟稳定，具备社区或官方支持；② 与项目技术栈兼容性高；③ 满足工业场景对实时性、可扩展性的要求。具体选型如下：

大语言模型方面，选用通义千问API（阿里百炼），用于维护建议生成，选择依据为中文工业场景理解能力强，API接口标准化，支持通过DashScope SDK调用。

深度学习框架方面，选用TensorFlow 2.16 / Keras，用于LSTM自编码器与LSTM回归模型的训练与推理，选择依据为提供完整的LSTM、Autoencoder构建接口，支持EarlyStopping等训练回调，模型以.keras格式保存便于跨平台部署。

数据科学工具方面，选用scikit-learn，用于随机森林、孤立森林、One-Class SVM等传统机器学习模型的实现及AUC、F1等评估指标计算，选择依据为标准化接口，与NumPy无缝衔接，joblib支持高效模型序列化。

可视化框架方面，选用Streamlit，用于诊断结果可视化与人机交互界面开发，选择依据为Python原生编写即可生成交互式Web界面，适合快速构建工程演示原型。

工业通信协议方面，选用MQTT，用于下位机传感器数据的实时接入及诊断结论的下发，选择依据为轻量级发布/订阅协议，适合工业现场低带宽、高可靠的通信需求。

工业自动化平台方面，选用施耐德EAE，用于PLC控制逻辑开发、诊断结论下发与工业系统联动，选择依据为与MQTT协议兼容，支持工业现场部署。

核心实验数据方面，最终采用NASA CMAPSS FD001涡扇发动机退化数据集作为系统落地的核心基准，该数据集提供完整的多传感器退化序列与真实RUL标签，是剩余寿命预测领域最广泛使用的工业级基准数据集之一。早期验证阶段使用CWRU轴承数据集（凯斯西储大学，12kHz驱动端振动信号）验证了多模型并行检测框架的可行性。

版本控制方面，选用Git，用于代码与模型产物的版本管理，支持快速回滚与迭代更新。

各工具协作关系：离线训练阶段，基于CMAPSS数据集，使用TensorFlow/Keras与scikit-learn完成多模型训练，产物通过Git管理；在线推理阶段，系统通过MQTT接收下位机数据，加载预训练模型完成并行推理与AUC加权融合，调用通义千问API生成维护建议，经Streamlit可视化呈现；系统集成阶段，通过MQTT与EAE平台对接，实现预测性维护系统与工业控制系统的联动。

参考文献

[1] Saxena A, Goebel K, Simon D, et al. Damage propagation modeling for aircraft engine run-to-failure simulation. 2008 International Conference on Prognostics and Health Management. IEEE, 2008: 1-9.

[2] Liu F T, Ting K M, Zhou Z H. Isolation forest. 2008 Eighth IEEE International Conference on Data Mining. IEEE, 2008: 413-422.

[3] Schölkopf B, Platt J C, Shawe-Taylor J, et al. Estimating the support of a high-dimensional distribution. Neural Computation, 2001, 13(7): 1443-1471.

[4] Breiman L. Random forests. Machine Learning, 2001, 45(1): 5-32.

[5] Hochreiter S, Schmidhuber J. Long short-term memory. Neural Computation, 1997, 9(8): 1735-1780.

[6] Zheng S, Ristovski K, Farahat A, et al. Long short-term memory network for remaining useful life estimation. 2017 IEEE International Conference on Prognostics and Health Management. IEEE, 2017: 88-95.

[7] Smith W A, Randall R B. Rolling element bearing diagnostics using the Case Western Reserve University data: A benchmark study. Mechanical Systems and Signal Processing, 2015, 64: 100-131.