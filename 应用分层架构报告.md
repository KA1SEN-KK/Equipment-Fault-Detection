# 设备故障检测系统分层架构报告

## 1. 报告目的
本文对当前设备故障检测应用进行分层梳理，明确每一层的职责、实现方式及对应源码模块。目标是提升系统可理解性、可维护性与后续扩展效率，并为项目汇报与答辩提供结构化材料。

---

## 2. 系统总体说明
本系统面向轴承振动信号场景，采用“多模型检测 + LLM融合决策”的工程化架构。与传统单模型或单路由方案相比，当前系统更强调：
- 多源证据联合诊断
- 可解释结论输出
- 分层解耦与可插拔扩展

系统数据流概述如下：
1. 前端接收振动信号输入
2. 特征层提取时频域统计特征
3. 模型层并行或按策略执行检测
4. Agent层进行融合决策与建议生成
5. 前端展示结构化结果

---

## 3. 分层架构与实现映射

### 3.1 表现层（UI / Interaction Layer）
**职责**
- 用户输入采集（信号文件、参数、模式选择、API Key）
- 检测结果可视化（状态、置信度、模型明细、建议）

**实现方式**
- 基于 Streamlit 快速构建交互界面
- 支持融合诊断模式与兼容模式切换

**对应源码模块**
- frontend/app.py
- main.py

---

### 3.2 应用编排层（Application Orchestration Layer）
**职责**
- 组织端到端流程：读取输入、构建上下文、调用Agent、渲染输出
- 管理配置与运行模式选择

**实现方式**
- 在前端流程中统一构建 DecisionContext
- 将业务流程与底层算法实现解耦

**对应源码模块**
- frontend/app.py
- main.py

---

### 3.3 Agent决策层（Agent Intelligence Layer）
**职责**
- 对模型结果进行策略编排与语义融合
- 生成解释与运维建议
- 提供失败回退，保障可用性

**实现方式**
- 主路径：FusionAgent 并行运行模型后由 LLM 做会诊融合
- 兼容路径：ReActAgent 路由式调用模型
- Prompt 模板化，确保输出结构化

**对应源码模块**
- agent/fusion_agent.py
- agent/react_agent.py
- agent/prompts.py
- agent/tools.py
- agent/llm.py

---

### 3.4 领域契约层（Domain Contract Layer）
**职责**
- 统一模型输入输出规范
- 定义配置与上下文数据结构
- 保证多模型可插拔一致性

**实现方式**
- 以抽象基类与数据类定义标准契约
- 所有 Runner 均遵循同一 predict 接口

**对应源码模块**
- models/base.py
- models/__init__.py

---

### 3.5 模型执行层（Model Execution Layer）
**职责**
- 执行具体算法推理并返回标准化结果
- 管理模型实例创建与注册

**实现方式**
- 采用“注册表 + Runner工厂”模式
- 同时支持无监督检测与有监督分类

**对应源码模块**
- models/registry.py
- models/lstm_autoencoder.py
- models/arima_runner.py
- models/isolation_forest_runner.py
- models/oneclass_svm_runner.py
- models/pca_runner.py
- models/kmeans_runner.py
- models/random_forest_runner.py
- models/cnn1d_runner.py
- models/placeholder_runners.py

---

### 3.6 特征与数据层（Feature & Data Layer）
**职责**
- 原始振动信号处理
- 时域与频域特征提取
- 数据加载、滑动窗口、数据集构建

**实现方式**
- FeaturePipeline 统一封装特征流程
- 工具函数支持模型侧高效复用

**对应源码模块**
- feature_engineering/pipeline.py
- feature_engineering/time_domain.py
- feature_engineering/frequency_domain.py
- models/_feature_utils.py
- utils/data_loader.py

---

### 3.7 训练与评估层（Offline Training & Evaluation Layer）
**职责**
- 离线训练模型与阈值
- 产物持久化（模型、scaler、meta）
- 离线评估与性能验证

**实现方式**
- 训练与线上推理解耦
- 线上仅加载训练产物进行检测

**对应源码模块**
- training/cwru_lstm_autoencoder.py
- utils/evaluation.py

---

## 4. 关键设计思路

### 4.1 分层解耦
系统将“交互、编排、决策、算法、特征、训练”职责明确隔离，降低跨模块耦合，便于多人协作与后续演进。

### 4.2 契约优先
通过统一 ModelRunner 协议，保障新增模型可低成本接入，并确保 Agent 层无需感知底层模型细节。

### 4.3 渐进增强
先构建传统可验证的检测能力，再引入 LLM 进行高层语义融合，降低全量依赖大模型的风险。

### 4.4 鲁棒性保障
在 LLM 不可用或返回异常时，系统具备规则回退路径，保证检测服务连续性。

### 4.5 可解释与可执行
输出不仅包含状态判定，还提供推理依据、模型分歧说明及运维建议，贴近现场应用需求。

---

## 5. 当前实现状态总结
当前系统已形成较完整的工程闭环：
- 前端可交互上传信号并展示诊断
- 特征层可稳定输出时频域统计特征
- 模型层支持多算法并行检测
- Agent层支持融合会诊与回退策略
- 训练层可独立产出可部署模型产物

系统整体已具备从实验验证走向工程应用的架构基础。

---

## 6. 结论
本项目架构可概括为：
“以分层解耦为骨架，以统一契约为接口，以多模型检测为能力底座，以 LLM 融合会诊为智能增强。”

该设计在可扩展性、可维护性、可解释性和工程可落地性方面具备明显优势，适合作为后续故障预测扩展、模型对比实验和产业化迭代的基础框架。