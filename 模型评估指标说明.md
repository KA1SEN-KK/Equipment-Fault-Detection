# 模型评估指标说明

本项目包含两个核心模型任务，评估方式不同。

---

## 一、故障检测模型（LSTM Autoencoder）

### 任务性质
无监督异常检测。模型仅在健康数据上训练，通过重建误差判断当前状态是否异常。

### 标签构造
CMAPSS 数据集本身不提供逐周期故障标签，需根据 RUL 构造：

```
RUL < 30 个周期  →  故障（label = 1）
RUL ≥ 30 个周期  →  正常（label = 0）
```

### 评估指标

| 指标 | 公式 | 说明 |
|---|---|---|
| Precision | TP / (TP + FP) | 报警中真正故障的比例 |
| Recall | TP / (TP + FN) | 所有故障中被检出的比例 |
| F1 Score | 2 × P × R / (P + R) | 综合指标 |
| AUC-ROC | — | 阈值无关的整体判别能力 |

**本任务中 Recall 优先于 Precision**：漏报故障（FN）的代价远高于误报（FP），宁可多报警也不能漏检。

### 运行评估
```bash
python -m training.cmapss_fault_detection eval \
    --data_dir CMAPSSData \
    --subset FD001 \
    --artifact_dir artifacts_cmapss_fd001
```

---

## 二、剩余使用寿命预测模型（LSTM 回归）

### 任务性质
有监督回归。输入最近 30 个周期的传感器数据，输出标量 RUL（剩余可用周期数）。

### 评估指标

#### 1. MAE（平均绝对误差）
$$\text{MAE} = \frac{1}{N} \sum_{i=1}^{N} |{\hat{y}_i - y_i}|$$

单位与 RUL 相同（周期数），直观反映平均预测偏差。越小越好。

#### 2. RMSE（均方根误差）
$$\text{RMSE} = \sqrt{\frac{1}{N} \sum_{i=1}^{N} (\hat{y}_i - y_i)^2}$$

对大误差更敏感，适合检验极端偏差。越小越好。

#### 3. NASA 评分函数（CMAPSS 官方标准）
$$S = \sum_{i=1}^{N} \begin{cases} e^{-d_i/13} - 1 & d_i < 0 \quad \text{（预测偏早，保守）} \\ e^{d_i/10} - 1 & d_i \geq 0 \quad \text{（预测偏晚，危险）} \end{cases}$$

其中 $d_i = \hat{y}_i - y_i$。

**非对称惩罚**：预测偏晚（认为设备还能撑很久但实际快故障了）惩罚更重，符合工程安全需求。越小越好。

#### 4. 转分类后的 F1（辅助）
将回归结果转为分类，评估预警能力：

```python
# 预测RUL < 50 → 预警，否则 → 正常
y_pred_cls = (rul_pred < 50).astype(int)
y_true_cls = (rul_true < 50).astype(int)
f1 = f1_score(y_true_cls, y_pred_cls)
```

### 运行评估
```bash
python -m training.cmapss_rul eval \
    --data_dir CMAPSSData \
    --subset FD001 \
    --artifact_dir artifacts_cmapss_rul_fd001
```

输出示例：
```json
{
  "test_mae": 18.4,
  "test_rmse": 24.7,
  "nasa_score": 3120.5,
  "n_engines": 100
}
```

---

## 三、指标汇总对比

| 任务 | 首要指标 | 次要指标 | 说明 |
|---|---|---|---|
| 故障检测 | Recall、F1 | Precision、AUC-ROC | 以不漏报为优先 |
| RUL 预测 | MAE、NASA Score | RMSE、分类F1 | NASA Score 为竞赛标准 |

---

## 四、CMAPSS FD001 参考基准

以下为文献中常见的 FD001 子集性能参考：

| 方法 | RMSE | NASA Score |
|---|---|---|
| 简单 LSTM | ~18 | ~300 |
| 双层 LSTM | ~14 | ~200 |
| Transformer | ~12 | ~150 |

本项目目标：RMSE < 20，NASA Score < 500。
