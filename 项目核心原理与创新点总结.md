# 项目核心原理、创新点与设计思路总结

## 1. 项目定位
本项目是一个面向设备振动信号的故障检测与诊断系统，核心目标是：
- 对上传的振动信号进行异常/故障识别
- 融合多模型结果，输出更稳定、更可解释的诊断结论
- 提供可执行的运维建议，提升工程落地价值

系统本质上属于故障检测（Fault Detection）范畴，并具备向预测性维护扩展的基础。

---

## 2. 核心原理

### 2.1 数据输入与预处理
- 输入为一维振动时序信号（.npy）
- 通过滑动窗口切片将长时序转为可建模的样本块
- 统一数据接口，供不同模型复用

相关代码：
- `utils/data_loader.py`
- `models/lstm_autoencoder.py`（窗口处理逻辑）

### 2.2 特征工程
系统自动提取时域与频域统计特征，为模型与融合层提供结构化证据。

时域特征（示例）：
- RMS
- 峰值、峰峰值
- 峰度、偏度
- 波峰因子、脉冲因子、波形因子

频域特征（示例）：
- 谱质心
- 谱峰频率
- 谱熵

相关代码：
- `feature_engineering/time_domain.py`
- `feature_engineering/frequency_domain.py`
- `feature_engineering/pipeline.py`

### 2.3 多模型检测层
项目采用统一模型协议，支持多种检测器并行或独立运行：
- 无监督：LSTM-AE、ARIMA、Isolation Forest、One-Class SVM、PCA、KMeans
- 有监督：Random Forest、1D-CNN（需训练产物）

相关代码：
- `models/base.py`（统一接口）
- `models/registry.py`（模型注册与创建）
- `models/*.py`（各模型 Runner）

### 2.4 融合决策层
当前核心策略为：
1. 先并行运行多个模型
2. 汇总每个模型的 score/status/raw
3. 结合信号统计特征进行融合
4. 由 LLM 输出结构化诊断（状态、置信度、故障类型、推理、建议、模型权重）

相关代码：
- `agent/fusion_agent.py`
- `agent/prompts.py`（融合提示词）

### 2.5 可解释输出与前端交互
- 前端展示信号特征摘要
- 展示融合诊断结果与模型明细
- 支持 API Key 配置及 LLM 回退策略

相关代码：
- `frontend/app.py`

---

## 3. 思路上的关键改变

### 之前
LLM 当路由器，先选一个模型再跑。

问题：
- 本质接近规则分流
- 信息不足时，LLM 难以做有效选择
- 价值容易退化为高延迟 if-else

### 现在
先让多个模型独立给出证据，再由 LLM 扮演资深诊断工程师做会诊融合。

优势：
- 充分利用多模型互补性
- 让 LLM 聚焦其强项：语义融合和解释
- 输出更接近真实运维决策过程

---

## 4. 创新点（系统级）

### 创新点 1：LLM 角色重构
从“模型路由器”升级为“证据融合器”，提升 LLM 有效使用率。

### 创新点 2：多源证据联合诊断
融合原始信号统计特征 + 多模型检测结果 + 上下文信息，实现结构化会诊输出。

### 创新点 3：统一协议与可插拔框架
基于统一 Runner 接口与注册表，低成本扩展新模型，具备良好工程演化能力。

### 创新点 4：鲁棒回退机制
当 LLM 不可用时，自动回退到规则融合，保证系统持续可用。

### 创新点 5：结果可解释、可执行
输出不仅有状态，还包含推理依据与运维建议，面向现场决策而非仅做离线评分。

---

## 5. 现有内容概览
当前已接入 8 个真实可用模型：
- 无监督：LSTM-AE、ARIMA、Isolation Forest、One-Class SVM、PCA、KMeans
- 有监督：Random Forest、1D-CNN（待训练产物）

特征工程：
- 自动提取 11 维时频域特征（如 RMS、峰度、谱熵等）

Agent 体系：
- FusionAgent（推荐）：多模型并行 + LLM 融合
- ReActAgent（兼容）：单模型路由流程

---

## 6. 设计思路（架构原则）

1. 分层解耦：数据、特征、模型、融合、展示分层清晰
2. 契约优先：统一输入输出协议，保障扩展一致性
3. 渐进增强：先保证传统检测基线，再引入 LLM 融合
4. 可靠性优先：外部依赖失败时可回退
5. 工程落地导向：强调可解释性、可维护性和可复用性

---

## 7. 一句话总结
这是一个以传统故障检测为基础、以 LLM 融合会诊为增强的工程化框架：
- 算法层负责检测能力
- 融合层负责综合研判与解释
- 系统层保证可扩展、可回退、可落地
